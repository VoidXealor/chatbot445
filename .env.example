# This application runs 100% locally and offline on-device via WebGPU / WebAssembly.
# Zero external cloud API keys or online third-party credentials are used.

# Optional Server Port (Defaults to 3000 in dev, Cloud Run sets this dynamically)
PORT=3000

# App URL (Automatically set by hosting environment if needed)
APP_URL=""
